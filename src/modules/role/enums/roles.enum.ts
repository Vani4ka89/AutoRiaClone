export enum ERoleSimple {
  BUYER = 'buyer',
  SELLER = 'seller',
}

export enum ERoleAccess {
  ADMIN = 'admin',
  MANAGER = 'manager',
}

export enum ERoleAll {
  ADMIN = 'admin',
  MANAGER = 'manager',
  BUYER = 'buyer',
  SELLER = 'seller',
}
